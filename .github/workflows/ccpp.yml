name: C/C++ CI

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest
    
    steps:
    - uses: actions/checkout@v1
    - name: ncurses
      run: apt-get install libncurses5-dev
    - name: make
      run: make

